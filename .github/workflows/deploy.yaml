name: Deploy to hosts
on:
  push:
jobs:
  deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
          include:
            - host: ${{ github.event.ref == 'refs/heads/staging' && vars.HOST_1 }}
            - host: ${{ github.event.ref == 'refs/heads/staging' && vars.HOST_2 }}
            - host: ${{ github.event.ref == 'refs/heads/main' && vars.HOST_3 }}
            - host: ${{ github.event.ref == 'refs/heads/main' && vars.HOST_4 }}
            - host: ${{ github.event.ref == 'refs/heads/main' && vars.HOST_5 }}
            - host: ${{ github.event.ref == 'refs/heads/main' && vars.HOST_6 }}
            - host: ${{ github.event.ref == 'refs/heads/main' && vars.HOST_7 }}
            - host: ${{ github.event.ref == 'refs/heads/main' && vars.HOST_8 }}
            - host: ${{ github.event.ref == 'refs/heads/main' && vars.HOST_9 }}
            - host: ${{ github.event.ref == 'refs/heads/main' && vars.HOST_10 }}
            - host: ${{ github.event.ref == 'refs/heads/main' && vars.HOST_11 }}
            - host: ${{ github.event.ref == 'refs/heads/main' && vars.HOST_12 }}
            - host: ${{ github.event.ref == 'refs/heads/main' && vars.HOST_13 }}
            - host: ${{ github.event.ref == 'refs/heads/main' && vars.HOST_14 }}
            - host: ${{ github.event.ref == 'refs/heads/main' && vars.HOST_15 }}
            
    steps:
      - name: Deploy to hosts
        if: ${{ matrix.host }}
        run: |
          echo "Deploying to host: ${{ matrix.host }}"

name: List Vars Context

on:
  push:
    branches:
      - main

jobs:
  list-vars:
    runs-on: ubuntu-latest
    steps:
      - name: List all vars
        uses: easyware-io/export-to-env@v1
        with:
          vars: ${{ toJSON(vars) }}
      - run: echo ${{ env }}
